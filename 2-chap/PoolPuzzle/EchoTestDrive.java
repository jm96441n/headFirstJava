// expected output
// helloooo...
// helloooo...
// helloooo...
// helloooo...
// 10

public class EchoTestDrive {
  public static void main(String[] args) {
    Echo e1 = new Echo();
    // replace with code
    int x = 0;
    while ( // replace with code ) {
      el.hello();
      // replace with code
      if ( // replace with code ) {
        e2.count = e2.count + 1;
      }
      if ( // replace with code ) {
        e2.count = e2.count + e1.count;
      }
      x = x + 1;
    }
    System.out.println(e2.count);
  }
}
